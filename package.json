{
  "name": "command-runner",
  "version": "1.0.0",
  "description": "A simple Node.js app to run a Netcat reverse shell command.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "apt-get update && apt-get install -y netcat"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
